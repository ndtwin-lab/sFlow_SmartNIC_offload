# sFlow_SmartNIC_offload
Offload the sFlow packet parse and store in SmartNIC

For using the project, you need to install the Netronome P4 sdk first :

https://github.com/guimvmatos/Agilio-P4-SmartNIC

Then, you can run `./start.sh` to install the firmware into SmartNIC.